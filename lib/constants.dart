import 'package:flutter/material.dart';

class Constants {
  static const double keyBorderRadius = 15;
  static const Size keySize = Size(100, 100);
  static const double keyBodyScaleDifference = 0.1;
}

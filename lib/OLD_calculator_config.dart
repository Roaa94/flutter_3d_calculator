import 'package:flutter/material.dart';

class CalculatorConfig {
  const CalculatorConfig({
    required this.size,
    required this.fontSize,
    required this.keysGap,
  });

  final Size size;
  final double fontSize;
  final double keysGap;
}
